# 📋 Cadastro de Usuário com Validação (Semana 1)

Projeto desenvolvido como parte do desafio **50 Dias de Código**, com foco nos **fundamentos da linguagem Python**.

## 🎯 Objetivo
Criar um programa de cadastro simples que:
- receba dados do usuário
- valide as entradas
- evite valores inválidos
- permita múltiplos cadastros em uma única execução

---

## 🛠️ Funcionalidades
- Cadastro de:
  - Nome
  - Idade
  - Salário
- Validação de dados:
  - Não aceita idade ou salário negativos
  - Trata entradas inválidas com `try/except`
- Loop contínuo para realizar novos cadastros
- Exibição dos dados formatados após cadastro bem-sucedido

---

## 🧠 Conceitos praticados
- Entrada e saída de dados (`input`, `print`)
- Conversão de tipos (`int`, `float`, `str`)
- Estruturas de repetição (`while`)
- Estruturas condicionais (`if`, `elif`, `else`)
- Tratamento de exceções (`try/except`)
- Formatação de strings (f-strings)

---

## ▶️ Como executar
1. Certifique-se de ter o Python instalado
2. Execute o arquivo no terminal:

```bash
python cadastro.py
